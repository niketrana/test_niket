pipeline {
    agent {
    environment {
        Go111MODULE='on'
    }
    stages {
        stage('Test') {
            steps {
                git 'https://github.com/niketrana/test_niket2.git'
            }
        }
    }
   }
}
